Environment variables:

- `CONAN_VS2026_BIN_DIR` default: `os.getenv("VCINSTALLDIR")`
- `CONAN_VS2022_BIN_DIR` default: `os.getenv("VCINSTALLDIR")`
- `CONAN_LLVM_BIN_DIR` default: `C:/Program Files/LLVM/bin`
- `CONAN_MSYS2_ROOT` default: `C:/msys64`
